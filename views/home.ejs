<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="row">
      <div class="col s12 m4 l2"><%- include('partials/navbar') %></div>
      <div class="col s12 m8 l9" style="margin-top: 1rem;">
        <% if(category === '') { %>
        <form action="inventory/category/create" method="POST" class="col s12">
        <% } else { %>
        <form action="" method="POST" class="col s12">
        <% } %>
          <div class="row">
            <div class="input-field col s8">
              <input placeholder="Category Name" type="text"
              class="validate" name="categoryname" 
              <% if(category === '') {%>
                value = '' 
              <%}else{%> 
                value="<%=category%>"
              <%}%> />
              <label for="first_name">Add new category</label>
              <% if(errors) { %>
                <ul>
                  <% errors.map(error=>{ %>
                    <li><%=error.msg%></li>
                  <% }) %>
                </ul>
              <% } %>
            </div>
            <button class="btn waves-effect" type="submit">Submit</button>
          </div>
        </form>
        <div class="row">
          <div class="col s12 m4">
            <div class="card teal darken-6">
              <div class="card-content white-text">
                <span class="card-title">Total Stocks</span>
                <h3><%=categories.length%></h3>
              </div>
            </div>
          </div>
          <div class="col s12 m4">
            <div class="card teal darken-6">
              <div class="card-content white-text">
                <span class="card-title">Empty Stocks</span>
                <h3><%=emptyCategories.length%></h3>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
